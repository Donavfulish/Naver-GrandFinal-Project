import express from 'express';

const router = express.Router();

// TODO: Implement search routes
// This is a placeholder to prevent import errors

router.get('/', (req, res) => {
  res.json({ message: 'Search routes - Coming soon' });
});

export default router;

